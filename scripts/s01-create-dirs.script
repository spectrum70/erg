# Sysam ERG
# *** source this file ***

mkdir -p targetfs
mkdir -p ${ERG}/targetfs/{bin,boot,dev,etc,home,lib/{firmware,modules}}
mkdir -p ${ERG}/targetfs/{mnt,opt,proc,sbin,srv,sys}
mkdir -p ${ERG}/targetfs/var/{cache,lib,local,lock,log,opt,run,spool}
install -d -m 0750 ${ERG}/targetfs/root
install -d -m 1777 ${ERG}/targetfs/{var/,}tmp
mkdir -p ${ERG}/targetfs/usr/{,local/}{bin,include,lib,sbin,share,src}
